export async function initMap(_values: any[]): Promise<void> {
    const mapElement = document.getElementById("map");
    if (mapElement) {
      // Inicialización del mapa y eventos aquí...
    } else {
      console.error("Elemento 'map' no encontrado");
    }
  }
  