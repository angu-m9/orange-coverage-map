import styled from "styled-components";

const StyledLogin = styled.div`

  h1{
    font-family: 'HelvNeue75_W1G', sans-serif;;
  }

  form{
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  

  .button{
    width: 5rem;
  }
`;

export default StyledLogin;
